{
  "name": "gift-stationery-shop",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend --include=dev",
    "db:init": "node backend/db_init.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
